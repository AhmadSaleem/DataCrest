<% if flash[:alert].blank? %>
  location.reload();
<% else %>
  $('.sign-in-body').html("<%= j render 'clients/sessions/login_fields' %>");
  $('.sign-in-body').prepend("<%= j render 'layouts/alerts' %>")
<% end %>
