<div class="sidebar" data-color="#1f77d0">
  <div class="sidebar-wrapper">
    <ul class="nav">
      <% if salesperson_signed_in? %>
        <% if current_salesperson.company_owner? %>
          <li>
            <%= link_to "#dashvariants", data: { toggle: "collapse" }, aria: { controls: "dashvariants", expanded: "false" } do %>
              Account Settings
            <% end %>
            <ul id="dashvariants" class="collapse list-group">
              <li class="list-styles">
                <%= link_to "Manage Employees", dashboard_salespeople_invitations_path %>
              </li>
              <li class="list-styles">
                <%= link_to "Manage Agents", new_dashboard_agent_invitation_path %>
              </li>
              <li class="list-styles">
                <%= link_to "Edit Account", edit_salesperson_registration_path %>
              </li>
            </ul>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
</div>
