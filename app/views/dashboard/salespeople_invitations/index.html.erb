<%= form_for @salesperson, url: dashboard_salespeople_invitations_path do |f| %>
  <%= render "shared/dashboard/employee_invitations/index", f: f %>
<% end %>

<% if @salespeople.present? %>
  <div class="row">
    <div class="col-12">
      <div class="card">
        <div class="header">
          <h3 class="title">Employees</h3>
        </div>

        <div class="content table-responsive table-full-width">
          <table class="table table-hover table-stripped">
            <thead>
              <th>Pic</th>
              <th>Name</th>
              <th>Email</th>
              <th>Invitation Status</th>
              <th>Action</th>
            </thead>

            <tbody>
              <% @salespeople.each do |salesperson| %>
                <tr>
                  <td><%= image_tag salesperson.profile_picture, style: 'max-width: 50px' %></td>
                  <td><%= salesperson.full_name %></td>
                  <td><%= salesperson.email %></td>
                  <td><%= salesperson.status %></td>
                  <td><%= render partial: "shared/dashboard/employee_invitations/actions", locals: { person: salesperson } %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>

      <div class="pull-right">
        <%= paginate @salespeople %>
        <%= page_entries_info @salespeople, entry_name: "employees" %>
      </div>
    </div>
  </div>
<% else %>
  <p>There are no outstanding invitations.</p>
<% end %>
